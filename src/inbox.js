import { main, mainTitle } from './index.js';

export default function showInbox() {
    mainTitle.textContent = 'inbox';
    const content = document.createDocumentFragment('div');
    content.innerHtml += {
        
    }
}